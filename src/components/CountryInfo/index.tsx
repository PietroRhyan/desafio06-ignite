import { Flex } from '@chakra-ui/react'

type CountryInfoProps = {
  value: number,
  title: string,
}

export function CountryInfo({ value, title }: CountryInfoProps) {
  return (
    <Flex >
      
    </Flex>
  )
}